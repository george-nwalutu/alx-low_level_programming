#include <stdio.h>

/**
 * main - program that prints all single digit numbers of base 10.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int z;

	for (z = 0; z < 10; z++)
	{
		printf("%d", z);
	}
	putchar('\n');
	return (0);
}
