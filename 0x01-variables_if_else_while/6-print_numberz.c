#include <stdio.h>

/**
 * main - Prints base 10 numbers from 0 to 9.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int s;

	for (s = 48; s < 58; s++)
	{
		putchar(s);
	}
	putchar('\n');
	return (0);
}
